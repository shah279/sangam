# Supabase REST endpoint, for example https://project-ref.supabase.co
SANGAM_SUPABASE_URL=

# Server-side secret/service-role key. Never ship this in the mobile app.
SANGAM_SUPABASE_KEY=

# Optional: read-only access to a separate Supabase project's NSE/BSE
# instrument master, used to widen symbol normalization beyond the curated
# alias table. That project's RLS is scoped to ONE dedicated Auth user (not
# to "anon" — anon must stay denied there), so this needs that user's own
# email/password plus the project's anon key (required by Supabase's gateway
# alongside the user's JWT). Never the service_role key.
SANGAM_BROKER_SUPABASE_URL=
SANGAM_BROKER_SUPABASE_ANON_KEY=
SANGAM_BROKER_SUPABASE_EMAIL=
SANGAM_BROKER_SUPABASE_PASSWORD=

SANGAM_GEMINI_KEY=
SANGAM_GEMINI_MODEL=gemini-3.1-flash-lite

# Optional
SANGAM_PROXY_URL=
SANGAM_LOOKBACK_HOURS=24
SANGAM_DISCOVERY_OVERLAP_HOURS=6
SANGAM_MAX_STAGE_ATTEMPTS=5
SANGAM_RETRY_BASE_MINUTES=15
SANGAM_CONSENSUS_MIN_CONFIDENCE=0.55
SANGAM_DAILY_REPORT_HOURS=24
SANGAM_DAILY_MAX_ITEMS=8
SANGAM_DAILY_MIN_CREATORS=2
SANGAM_REPORT_DIR=reports
SANGAM_FFMPEG_BIN=ffmpeg
